package it.attendance100.mybicocca.ui.screen.registry.subscreen.booking.state

/** The two pages of the per-appello booking flow: the call detail, then the confirm step. */
enum class BookingSheetStep { Info, Confirm }
