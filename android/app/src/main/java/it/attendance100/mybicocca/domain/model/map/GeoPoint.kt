package it.attendance100.mybicocca.domain.model.map

/** A WGS84 latitude/longitude pair; locates a campus building's pin on the map tab. */
data class GeoPoint(val latitude: Double, val longitude: Double)
