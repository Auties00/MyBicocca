<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.USE_BIOMETRIC" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <!-- Scanning the lesson QR code when registering presence. -->
  <uses-permission android:name="android.permission.CAMERA" />
  <!-- EasyBadge presence certification attaches the device's coordinates. -->
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />
  <uses-permission
          android:name="android.permission.WRITE_EXTERNAL_STORAGE"
          android:maxSdkVersion="29" />

  <uses-feature android:name="android.hardware.camera.any" android:required="false" />
  <uses-feature android:name="android.hardware.location" android:required="false" />

  <queries>
    <intent>
      <action android:name="android.speech.RecognitionService" />
    </intent>
    <package android:name="com.microsoft.office.word" />
    <package android:name="com.microsoft.office.excel" />
    <package android:name="com.microsoft.office.powerpoint" />
  </queries>

  <uses-sdk tools:overrideLibrary="com.google.mlkit.genai.speechrecognition,com.google.mlkit.genai.common" />

  <application
      android:name=".MyBicoccaApplication"
      android:allowBackup="false"
      android:enableOnBackInvokedCallback="true"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:label="@string/app_name"
      android:localeConfig="@xml/locales_config"
      android:supportsRtl="true"
      android:theme="@style/Theme.MyBicocca.AnimatedSplash"
      tools:targetApi="33">
    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />

    <!-- Internal build counter from version.properties; read by core/version/BuildNumber.kt -->
    <meta-data
            android:name="it.attendance100.mybicocca.BUILD_NUMBER"
            android:value="${buildNumber}" />

    <activity
        android:name="it.attendance100.mybicocca.MyBicoccaActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:supportsPictureInPicture="true"
        android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|locale|layoutDirection|uiMode"
        android:theme="@style/Theme.MyBicocca.AnimatedSplash">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <!-- Scanning a mod_attendance QR outside the app opens it here. Not autoVerified
           (we can't host assetlinks on elearning.unimib.it), so Android offers MyBicocca
           in the chooser rather than claiming the link silently. -->
      <intent-filter android:label="@string/app_name">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" android:host="elearning.unimib.it"
            android:path="/mod/attendance/attendance.php" />
        <data android:scheme="http" />
      </intent-filter>
      <!-- Affluences reservation cancellation links emailed after a library booking. Not
           autoVerified (we don't own affluences.com), so Android offers MyBicocca in the chooser. -->
      <intent-filter android:label="@string/app_name">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" android:host="affluences.com"
            android:pathPrefix="/it/reservation/cancel" />
      </intent-filter>
    </activity>

    <!-- Crash screen host, launched by GlobalExceptionHandler on an uncaught exception. Its own
         process so it survives the main process's death; plain theme since the splash pipeline
         never runs here. -->
    <activity
            android:name="it.attendance100.mybicocca.CrashActivity"
            android:exported="false"
            android:process=":crash"
            android:theme="@style/Theme.MyBicocca" />

    <service
        android:name=".ui.screen.elearning.subscreen.videoPlayer.player.VideoPlaybackService"
        android:exported="false"
        android:foregroundServiceType="mediaPlayback">
      <intent-filter>
        <action android:name="androidx.media3.session.MediaSessionService" />
      </intent-filter>
    </service>

    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths" />
    </provider>

    <service
        android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
        android:enabled="false"
        android:exported="false">
      <meta-data
          android:name="autoStoreLocales"
          android:value="true" />
    </service>

    <activity
        android:name="com.google.android.gms.oss.licenses.OssLicensesMenuActivity"
        android:theme="@style/Theme.MyBicocca.OssLicenses"
        tools:replace="android:theme" />

    <activity
        android:name="com.google.android.gms.oss.licenses.OssLicensesActivity"
        android:theme="@style/Theme.MyBicocca.OssLicenses"
        tools:replace="android:theme" />

    <provider
        android:name="androidx.startup.InitializationProvider"
        android:authorities="${applicationId}.androidx-startup"
        android:exported="false"
        tools:node="merge">
      <meta-data
          android:name="androidx.work.WorkManagerInitializer"
          android:value="androidx.startup"
          tools:node="remove" />
    </provider>

    <receiver android:name=".data.update.InstallResultReceiver" android:exported="false" />
  </application>
</manifest>
