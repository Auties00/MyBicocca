<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.USE_BIOMETRIC" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
  <!-- Search dictation (mic streaming into the unified search field). -->
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <!-- Legacy write access; ignored on API 30+ (scoped storage), so capped to API 29. -->
  <uses-permission
          android:name="android.permission.WRITE_EXTERNAL_STORAGE"
          android:maxSdkVersion="29" />

  <!-- Package visibility for the platform speech recognizer (API 30+ filters services). -->
  <queries>
    <intent>
      <action android:name="android.speech.RecognitionService" />
    </intent>
    <!-- Office documents hand off to Microsoft's apps via their ms-* protocol handlers;
         PackageManager checks need explicit visibility on API 30+. -->
    <package android:name="com.microsoft.office.word" />
    <package android:name="com.microsoft.office.excel" />
    <package android:name="com.microsoft.office.powerpoint" />
  </queries>

  <!-- ML Kit GenAI declares minSdk 26; the app supports 25. Safe to override: every GenAI
       call site guards on Build.VERSION >= 26 and falls back to the deterministic path.
       androidx.pdf declares minSdk 28; the PDF viewer gates on Build.VERSION >= 28 and
       falls back to a PdfRenderer-based pager below that. -->
  <uses-sdk tools:overrideLibrary="com.google.mlkit.genai.prompt,com.google.mlkit.genai.speechrecognition,com.google.mlkit.genai.common,androidx.pdf,androidx.pdf.compose,androidx.pdf.viewer,androidx.pdf.document.service" />

  <application
    android:name=".MyBicoccaApplication"
    android:allowBackup="false"
    android:enableOnBackInvokedCallback="true"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:label="@string/app_name"
          android:localeConfig="@xml/locales_config"
    android:supportsRtl="true"
          android:theme="@style/Theme.MyBicocca.AnimatedSplash"
    tools:targetApi="33">
    <meta-data
      android:name="com.google.android.geo.API_KEY"
      android:value="${MAPS_API_KEY}" />
    <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    <activity
      android:name="it.attendance100.mybicocca.MyBicoccaActivity"
      android:exported="true"
      android:supportsPictureInPicture="true"
      android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.MyBicocca.AnimatedSplash">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <service
      android:name=".ui.screen.elearning.subscreen.videoPlayer.player.VideoPlaybackService"
      android:exported="false"
      android:foregroundServiceType="mediaPlayback">
      <intent-filter>
        <action android:name="androidx.media3.session.MediaSessionService" />
      </intent-filter>
    </service>

    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.fileprovider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths" />
    </provider>

    <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
      <meta-data
              android:name="autoStoreLocales"
              android:value="true" />
    </service>

    <activity
            android:name="com.google.android.gms.oss.licenses.OssLicensesMenuActivity"
            android:theme="@style/Theme.MyBicocca.OssLicenses"
            tools:replace="android:theme" />

    <activity
            android:name="com.google.android.gms.oss.licenses.OssLicensesActivity"
            android:theme="@style/Theme.MyBicocca.OssLicenses"
            tools:replace="android:theme" />
  </application>

</manifest>
